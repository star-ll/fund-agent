根据以下用户档案，用 Discord Markdown 格式输出持仓概览，不使用 ANSI 转义码。

**输出结构（严格按此顺序）：**

1. 标题行：`**我的持仓**`
2. 基本信息（如有）：风险偏好、投资年限、目标收益、可承受亏损
3. 每只基金一个代码块卡片，格式如下：

```
[基金名称或代码]
代码：xxxxxx
份额：xxx 份（如有）
成本：x.xxxx（如有）
备注：xxx（如有）
```

4. 如有 notes 字段，最后输出 `**备注：**` + notes 内容

**格式规范：**
- 若某字段无数据则不输出该行
- 语言简洁，不做分析和建议，只展示数据
- 总长度不超过 1800 字符

用户档案：
{{profile}}
